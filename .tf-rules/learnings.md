## Recent
[chat] createScrollProgress (story-scroll-utils.js) reports progress=1 immediately when totalDistance <= 0 (section scrollHeight <= viewport). Never use it for non-pinned 100svh sections (e.g. cinematic entry hero) — fade targets get opacity 0 on load. Use a local scroll handler based on -rect.top / rect.height instead.
[chat] Client prefers the story vessel/line animations as natural-flow multi-screen canvases (stacked 100svh screens, fixed-pixel SVG pieces with pathLength="1" + div line segments) over PINNED scroll-scrub sections. Draw-in mechanism evolved: was one-shot IntersectionObserver, now a bidirectional scroll scrub (pen at 88% viewport) — see later entry. A single viewport-filling viewBox SVG with preserveAspectRatio slice crops unpredictably per aspect ratio and breaks text/artwork registration — avoid it for line-art that must align with text.
[chat] Vessel line-art specs from client: heartbeat starts ≥80px below the sticky subnav; 7-strand tree = center + 3/side at ~52px spacing (center strand always perfectly straight); strands cut off above text (no converge), restart below it, and only merge at the bottom.
[chat] Story sections expose anchors ONLY as data-anchor attributes (no id) — any anchor lookup (subnav scroll-spy/click) must query [data-anchor="…"]:not(.story-subnav__pill), not getElementById; the subnav pills carry the same data-anchor values and appear earlier in the DOM, so an unscoped query returns the pill itself. Also .shopify-section story wrappers are position:relative, so target.offsetTop ≈ 0 — compute scroll targets with getBoundingClientRect().top + window.scrollY.
[chat] A div with height:100% inside a flex-grown wrapper (indefinite height) measures offsetHeight 0 in Chrome — scrub/draw JS that divides by measured height silently skips it forever. Use height:auto + align-self:stretch on the child (and a parentElement.offsetHeight fallback in JS) for grow-to-fill vessel line segments.
[chat] Client confirmed the universal-truth vessel/tree must scrub with scroll BOTH directions and replay every pass (not one-shot) — the pen-at-88%-viewport model in story-universal-truth.js is the accepted approach.
